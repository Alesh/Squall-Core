/**
 * Module _squall is the Python extension which implements
 * the event dispatcher and the async network primitives.
 */
#include <boost/python.hpp>
#include <squall/Squall.hxx>


BOOST_PYTHON_MODULE(_squall) {

    static_assert(false, "Using failback module");
}
